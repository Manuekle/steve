node_modules
.env*
!.env.example
.eve
.vercel
.workflow-data
.next
.output
.nitro
dist
.DS_Store
*.tsbuildinfo
logs

# Ansible deploy tooling
deploy/.galaxy
deploy/inventory.ini
deploy/*.retry

# Reference checkout (Houston), kept out of the build and the repo
que-podemos-sacar-de-este-repositorio
