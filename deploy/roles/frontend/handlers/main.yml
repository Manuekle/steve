---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart senka-web
  ansible.builtin.systemd:
    name: senka-web
    state: restarted
  become: true
