import { webSearch } from "eve/tools/defaults";

// Re-enable provider web search. The agent uses it for market research,
// competitor lookups, and answering business questions beyond the sandbox.
export default webSearch;
