import type { ReactNode } from "react";

export const metadata = { title: "Acceso — senka", robots: { index: false, follow: false } };

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
